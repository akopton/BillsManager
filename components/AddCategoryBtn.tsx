import { TouchableOpacity, Text } from "react-native"

export const AddCategoryBtn = ( ) => {

    return(
        <TouchableOpacity>
            <Text style={{textAlign: 'center'}}>elo mordo</Text>
        </TouchableOpacity>
    )
}